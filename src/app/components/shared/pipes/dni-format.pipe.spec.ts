import { DniFormatPipe } from './dni-format.pipe';

describe('DniFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DniFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
